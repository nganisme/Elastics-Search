# Elasticsearch Dockerfile

# Use an official Elasticsearch image as base
FROM bitnami/elasticsearch:8.13.4

# Set environment variables
ENV discovery.type=single-node
# ENV xpack.security.enabled=false

# Expose port
EXPOSE 9200
